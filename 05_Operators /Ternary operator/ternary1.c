// 2) example
#include<stdio.h>

int main(){
    int p = 10, q = 9; 
   
    (p < q) ? printf("p is less than q is %d < %d\n", p, q) : printf("p is greater than q is %d > %d\n", p, q);
    
    return 0;
}